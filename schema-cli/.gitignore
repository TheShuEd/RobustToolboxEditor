bin/
obj/
publish/

# Runtime no-op cache sidecar. The committed fixture is metadata.json alone;
# the fingerprint is machine/build-specific and lives only next to real output.
fixtures/metadata.fingerprint
