// Minimal stand-ins for the SS14 runtime handle types. FieldExtractor
// recognises them by fully-qualified name only, so we just need types
// living in the matching namespace.
namespace Robust.Shared.GameObjects;

internal readonly struct EntityUid { }
internal readonly struct NetEntity { }
